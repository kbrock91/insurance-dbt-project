version: 2

models:
  - name: dim_date
  - name: dim_agency
  - name: dim_policyholder
  - name: dim_product
  - name: dim_vehicle
  - name: dim_policy
  - name: fct_claims
    config:
      freshness: 
        build_after:  # build this model no more often than every X amount of time, as long as it has new data. Available only on dbt platform Enterprise tiers. 
          count: 0
          period: minute
          updates_on: all  # optional config
  - name: fct_premiums


