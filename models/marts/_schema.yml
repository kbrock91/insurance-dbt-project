version: 2

models:
  - name: metricflow_time_spine
    description: Time spine table for MetricFlow semantic layer
    columns:
      - name: date_day

  - name: dim_date
  - name: dim_agency
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - agency_id
              - agency_name
    columns:
      - name: agency_id

  - name: dim_policyholder
  - name: dim_product
  - name: dim_vehicle
  - name: dim_policy
  - name: fct_claims
  - name: fct_premiums
  - name: fct_policy_renewals